import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        margin: 20,
        marginTop: 50,
    },
    text: {
        color: 'white',
        fontSize: 30,
    },
    titleText: {
        fontSize: 30,
        fontWeight: 'bold',
        marginBottom: 20,
    },
  });
  